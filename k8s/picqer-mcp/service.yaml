apiVersion: v1
kind: Service
metadata:
  name: picqer-mcp
  namespace: pocharlies-rag
  labels:
    app: picqer-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8001
      targetPort: 8001
  selector:
    app: picqer-mcp
