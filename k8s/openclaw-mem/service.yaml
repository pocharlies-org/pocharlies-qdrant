apiVersion: v1
kind: Service
metadata:
  name: openclaw-mem
  namespace: pocharlies-rag
  labels:
    app: openclaw-mem
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: openclaw-mem
