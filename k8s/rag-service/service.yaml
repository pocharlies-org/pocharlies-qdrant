apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: pocharlies-rag
  labels:
    app: rag-service
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    app: rag-service
