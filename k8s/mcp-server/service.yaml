apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: pocharlies-rag
  labels:
    app: mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app: mcp-server
